[pytest]
# Only `tests/` is collected. The `test_*.py` scripts at the service root are
# hand-run analysis harnesses, not suites, and would otherwise execute on import.
testpaths = tests
pythonpath = .
addopts = -q
asyncio_mode = auto
